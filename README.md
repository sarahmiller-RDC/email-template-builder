
  # Email Template Builder

  This is a code bundle for Email Template Builder. The original project is available at https://www.figma.com/design/QrKgtzz8ecKDsVYuuMtWFz/Email-Template-Builder.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  